# 🔧 GitHub Setup Guide

## Настройка GitHub Secrets

Перейди в свой репозиторий → **Settings** → **Secrets and variables** → **Actions**

### Добавь следующие секреты:

| Secret Name | Value | Описание |
|-------------|-------|----------|
| `LAUNCHER_SERVER_URL` | `http://138.124.0.8` | URL твоего сервера |
| `CI_API_KEY` | `gh-actions-secret-key-12345` | API ключ для CI/CD |

### Как добавить секрет:

1. Нажми **"New repository secret"**
2. Введи **Name**: `LAUNCHER_SERVER_URL`
3. Введи **Value**: `http://138.124.0.8`
4. Нажми **"Add secret"**
5. Повтори для `CI_API_KEY`

## Workflows

После настройки секретов, эти workflows будут работать автоматически:

### `deploy.yml` - Автодеплой
- Срабатывает при пуше в `main`
- Билдит CS2 Internal/External
- Загружает на сервер автоматически

### `build.yml` - Билд лаунчера
- Билдит лаунчер для Windows
- Создаёт артефакты

## Генерация лицензионных ключей

### Через API (рекомендуется)

```bash
# Логин как админ
TOKEN=$(curl -s -X POST http://138.124.0.8/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}' | jq -r '.token')

# Создать 1 ключ на 30 дней для CS2
curl -X POST http://138.124.0.8/api/admin/licenses \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"game_id":"cs2","days":30}'

# Создать 10 ключей lifetime для CS2
curl -X POST http://138.124.0.8/api/admin/licenses \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"game_id":"cs2","count":10}'

# Создать 5 ключей на 7 дней с кастомным префиксом
curl -X POST http://138.124.0.8/api/admin/licenses \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"game_id":"cs2","days":7,"count":5,"prefix":"TRIAL"}'
```

### Типы подписок

| Days | Тип |
|------|-----|
| `null` или `0` | Lifetime (навсегда) |
| `1` | 1 Day |
| `7` | 1 Week |
| `30` | 1 Month |
| `90` | 3 Months |
| `365` | 1 Year |

## Формат ключей

```
CS2-A1B2-C3D4-E5F6      # Стандартный CS2 ключ
TRIAL-1234-5678-9ABC    # С кастомным префиксом
DAYZ-XXXX-XXXX-XXXX     # Для DayZ
```

## Процесс для нового пользователя

1. **Админ генерирует ключ** через API
2. **Пользователь получает ключ** (email, Discord, покупка)
3. **Пользователь открывает лаунчер**
4. **Нажимает "Register with key"**
5. **Вводит username, password, license key**
6. **Аккаунт создаётся + лицензия активируется**
7. **Готово!** Можно пользоваться

## Схема работы

```
┌─────────────────────────────────────────────────────────────┐
│                        АДМИН                                │
│  Генерирует ключи через API → CS2-XXXX-XXXX-XXXX           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      ПРОДАЖА/РАЗДАЧА                        │
│  Discord, Telegram, сайт, Sellix, Shopify...               │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      ПОЛЬЗОВАТЕЛЬ                           │
│  1. Запускает Launcher                                      │
│  2. Register → вводит ключ                                  │
│  3. Аккаунт создан, лицензия активирована                  │
│  4. Launch CS2 → скачивает чит → играет                    │
└─────────────────────────────────────────────────────────────┘
```

