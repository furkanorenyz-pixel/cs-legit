# 📖 Документация Single-Project

Добро пожаловать в документацию проекта! Здесь собраны все необходимые материалы для разработки, развёртывания и поддержки системы.

---

## 📚 Содержание

### 🚀 Для начинающих
- **[QUICK_START.md](QUICK_START.md)** - Быстрый старт для разработчиков, администраторов и пользователей

### 🏗️ Архитектура
- **[ARCHITECTURE.md](ARCHITECTURE.md)** - Полная архитектура системы, схемы потоков, база данных
- **[PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md)** - Структура проекта, описание компонентов, версионирование

### 🔧 Администрирование
- **[ADMIN_PANEL.md](ADMIN_PANEL.md)** - Инструкция по работе с админ-панелью
- **[DEPLOYMENT.md](launcher-server/docs/DEPLOYMENT.md)** - Развёртывание сервера на VPS

### 🐛 Решение проблем
- **[TROUBLESHOOTING.md](TROUBLESHOOTING.md)** - Диагностика и исправление типичных проблем

### ⚙️ Настройка
- **[GITHUB_SETUP.md](GITHUB_SETUP.md)** - Настройка CI/CD, GitHub Actions, секреты

---

## 📂 Структура проекта

```
externa-cheat/
├── launcher/              # GUI лаунчер (C++ + ImGui)
├── launcher-server/       # Backend + Website (Node.js + SQLite)
├── externa/               # External чит для CS2
├── hypervisor-cheat/      # Гипервизорный чит (Ring -1)
├── kernel/                # Kernel driver (Ring 0)
├── .github/workflows/     # CI/CD автоматизация
└── docs/                  # Документация (вы здесь)
```

---

## 🎯 Основные компоненты

### 1. Launcher (Клиент)
- **Язык:** C++17
- **UI:** ImGui + DirectX 11
- **Функции:** Авторизация, управление лицензиями, загрузка читов, автообновление
- **Билд:** GitHub Actions → загрузка на сервер

### 2. Backend Server
- **Язык:** Node.js 18+
- **База:** SQLite
- **API:** REST (JWT авторизация)
- **Функции:** Аутентификация, раздача файлов, управление лицензиями, мониторинг обновлений CS2

### 3. Website
- **Технология:** Статичный HTML (без фреймворков)
- **Дизайн:** Градиенты, анимации, адаптивный дизайн
- **Функции:** Скачивание лаунчера, проверка статуса игр

---

## 🔄 Жизненный цикл обновления

```
1. Developer → git push (изменения)
2. GitHub Actions → компиляция
3. CI → загрузка на сервер (/api/admin/ci/upload)
4. Server → шифрование + сохранение в storage/
5. Server → обновление версии в базе
6. Launcher → проверка версии (/api/games/status)
7. Launcher → скачивание обновления
8. Launcher → применение через update.bat
```

---

## 🔐 Безопасность

### Защита Launcher
- ✅ VM Detection (VMware, VirtualBox, Hyper-V)
- ✅ Debugger Detection (IsDebuggerPresent)
- ✅ HWID Lock (привязка к железу)
- ✅ Integrity Checks (опционально)

### Защита Server
- ✅ JWT Authentication (токены с истечением)
- ✅ Rate Limiting (защита от DDoS)
- ✅ File Encryption (AES-256-CBC)
- ✅ HTTPS (SSL сертификаты)

---

## 📡 API Endpoints

### Публичные
- `GET /` - главная страница
- `GET /api/games/status` - статус игр
- `GET /api/download/launcher` - скачать лаунчер
- `POST /api/auth/login` - вход
- `POST /api/auth/register` - регистрация

### С авторизацией (JWT)
- `GET /api/auth/me` - профиль пользователя
- `POST /api/auth/activate` - активировать лицензию
- `GET /api/download/:game/external` - скачать чит

### Админские
- `POST /api/admin/licenses` - создать ключи
- `POST /api/admin/games/:id/status` - изменить статус игры
- `DELETE /api/admin/users/:id/hwid` - сбросить HWID

### CI/CD
- `POST /api/admin/ci/upload` - загрузить билд (GitHub Actions)
- `POST /api/admin/reload` - перезагрузить сервер

---

## 🚨 Частые проблемы

| Проблема | Документ |
|----------|----------|
| Launcher показывает "v2" | [TROUBLESHOOTING.md](TROUBLESHOOTING.md#-launcher-v2-вместо-v20x) |
| Бесконечное "Update Required" | [TROUBLESHOOTING.md](TROUBLESHOOTING.md#-бесконечное-update-required) |
| Красный статус на сайте | [TROUBLESHOOTING.md](TROUBLESHOOTING.md#-статус-на-сайте-красный) |
| Download limit exceeded | [TROUBLESHOOTING.md](TROUBLESHOOTING.md#-download-limit-exceeded) |
| SSH connection closed | [TROUBLESHOOTING.md](TROUBLESHOOTING.md#-ssh-connection-closed) |

---

## 🛠️ Для разработчиков

### Локальная разработка

**Launcher:**
```bash
cd launcher
cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Release
cmake --build build
./build/launcher.exe
```

**Backend:**
```bash
cd launcher-server/backend
npm install
npm run dev  # или node src/index.js
```

### Git Workflow
```bash
# 1. Создать ветку для фичи
git checkout -b feature/new-feature

# 2. Внести изменения
git add .
git commit -m "feat: описание изменений"

# 3. Пушить
git push origin feature/new-feature

# 4. Создать Pull Request на GitHub
```

### Запуск тестов
```bash
# Backend
cd launcher-server/backend
npm test

# Launcher (пока нет unit-тестов)
# Ручное тестирование через запуск
```

---

## 📊 Мониторинг

### Проверка состояния
```bash
# Здоровье сервера
curl http://single-project.duckdns.org/health

# Статус игр
curl http://single-project.duckdns.org/api/games/status | jq

# Логи сервера
ssh root@138.124.0.8 "journalctl -u launcher -n 100"
```

### Метрики
```bash
# Статистика через API (требует admin токен)
curl -H "Authorization: Bearer <token>" \
  http://single-project.duckdns.org/api/admin/stats
```

---

## 🔗 Полезные ссылки

- **Сайт:** http://single-project.duckdns.org
- **Админ-панель:** http://single-project.duckdns.org/panel
- **GitHub:** https://github.com/gavrikov2044-bot/cs-legit
- **CI/CD:** https://github.com/gavrikov2044-bot/cs-legit/actions

---

## 📝 Версионирование

### Launcher
- **Формат:** `2.0.X` где X = GitHub run_number
- **Сборка:** автоматическая при push в `launcher/`

### Cheats
- **Формат:** `1.0.X` где X = GitHub run_number
- **Сборка:** автоматическая при push в `externa/`

### Backend
- **Формат:** `1.0.0` (ручное обновление)
- **Деплой:** через webhook `/api/admin/reload`

---

## 🆘 Поддержка

Если возникли вопросы:

1. **Проверить документацию** - скорее всего ответ уже есть
2. **Посмотреть логи** - большинство проблем видны в логах
3. **Открыть Issue** на GitHub с описанием и скриншотами

---

## 📅 История обновлений

### v2.0.47 (23.12.2025)
- ✅ Исправлена синхронизация версий CI → Server → Launcher
- ✅ Убрана проблема с "v2" в нижней части окна
- ✅ Исправлен бесконечный цикл обновлений
- ✅ Улучшен UI лаунчера (градиенты, анимации)
- ✅ Создана полная документация проекта

### v2.0.42 (22.12.2025)
- ✅ Первая стабильная версия с автообновлением
- ✅ Реализована система лицензий
- ✅ Добавлен Telegram monitor для CS2

---

**Последнее обновление:** 23.12.2025  
**Версия документации:** 1.0

