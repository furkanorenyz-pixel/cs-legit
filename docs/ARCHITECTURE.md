# 🏗️ Архитектура Single-Project

## 📊 Общая Схема

```
┌──────────────────────────────────────────────────────────────┐
│                         ПОЛЬЗОВАТЕЛЬ                          │
└────────────┬────────────────────────────────┬─────────────────┘
             │                                │
             │ (1) Скачивает                 │ (2) Запускает
             │                                │
    ┌────────▼────────┐              ┌───────▼────────┐
    │   WEBSITE       │              │   LAUNCHER     │
    │   (index.html)  │              │   (GUI App)    │
    └────────┬────────┘              └───────┬────────┘
             │                                │
             │                                │ API Requests
             │                                │ (JWT Auth)
             │                                │
    ┌────────▼────────────────────────────────▼─────────┐
    │            BACKEND SERVER                         │
    │         (Node.js + Express + SQLite)              │
    │                                                    │
    │  • Authentication (JWT)                           │
    │  • License Management                             │
    │  • Game Status                                    │
    │  • File Downloads (Encrypted)                     │
    │  • Telegram Monitor (CS2 updates)                 │
    └────────┬──────────────────────────┬────────────────┘
             │                          │
             │ (3) Скачивает чит       │ (4) CI Upload
             │                          │
    ┌────────▼────────┐         ┌──────▼───────────────┐
    │  STORAGE        │         │   GITHUB ACTIONS     │
    │  (Encrypted)    │         │   (CI/CD)            │
    │                 │         │                      │
    │  • launcher/    │         │  • launcher.yml      │
    │  • cs2/         │         │  • build.yml         │
    │  • offsets/     │         │  • deploy-server.yml │
    └─────────────────┘         └──────────────────────┘
```

---

## 🔐 Поток Аутентификации

```
┌─────────┐      (1) Login       ┌──────────┐
│ Launcher│ ───────────────────> │  Server  │
└─────────┘   user + pass + hwid └──────────┘
     │                                  │
     │        (2) Check DB              │
     │            • user exists?        │
     │            • password match?     │
     │            • hwid match or null? │
     │                                  │
     │<──────── (3) Return JWT ─────────│
     │         + licenses info          │
     │                                  │
     │        (4) Save session          │
     │        (token + username)        │
     │                                  │
     │     (5) Future requests          │
     │  ───────────────────────────────>│
     │   Header: Authorization: Bearer  │
```

---

## 🎮 Поток Запуска Чита

```
┌─────────┐                              ┌──────────┐
│ Launcher│                              │  Server  │
└────┬────┘                              └─────┬────┘
     │                                         │
     │  (1) GET /api/auth/me                  │
     │─────────────────────────────────────>  │
     │                                         │
     │  (2) Return licenses                   │
     │  <─────────────────────────────────────│
     │      { cs2: { expires_at, ... } }      │
     │                                         │
     │  (3) Check game.hasLicense ✓           │
     │                                         │
     │  (4) Check game status                 │
     │  GET /api/games/status                 │
     │─────────────────────────────────────>  │
     │                                         │
     │  (5) Return status                     │
     │  <─────────────────────────────────────│
     │      { cs2: { status: "operational" }} │
     │                                         │
     │  (6) User clicks "LAUNCH"              │
     │                                         │
     │  (7) GET /api/download/cs2/external    │
     │─────────────────────────────────────>  │
     │                                         │
     │  (8) Return encrypted .exe             │
     │  <─────────────────────────────────────│
     │                                         │
     │  (9) Decrypt + Save to %APPDATA%       │
     │                                         │
     │  (10) Check if cs2.exe running ✓       │
     │                                         │
     │  (11) CreateProcess(cs2_external.exe)  │
     │                                         │
     └──> Cheat Running                       │
```

---

## 🔄 Поток Обновления Лаунчера

```
┌─────────┐                              ┌──────────┐
│ Launcher│                              │  Server  │
└────┬────┘                              └─────┬────┘
     │                                         │
     │  (1) CheckForUpdates() каждые 30 сек   │
     │  GET /api/games/status                 │
     │─────────────────────────────────────>  │
     │                                         │
     │  (2) Return launcher.version           │
     │  <─────────────────────────────────────│
     │      { launcher: { version: "2.0.47" }}│
     │                                         │
     │  (3) Compare:                          │
     │      LAUNCHER_VERSION (2.0.46)         │
     │      < server (2.0.47)                 │
     │                                         │
     │  (4) Show "Update Required" modal      │
     │      (blocks all UI, only "UPDATE")    │
     │                                         │
     │  (5) User clicks "UPDATE NOW"          │
     │                                         │
     │  (6) GET /api/download/launcher        │
     │─────────────────────────────────────>  │
     │                                         │
     │  (7) Download to launcher_new.exe      │
     │  <─────────────────────────────────────│
     │                                         │
     │  (8) Create update.bat:                │
     │      • taskkill /IM launcher.exe       │
     │      • del launcher.exe                │
     │      • ren launcher_new.exe → launcher │
     │      • start launcher.exe              │
     │                                         │
     │  (9) Run update.bat + exit             │
     │                                         │
     └──> Launcher restarts with new version  │
```

---

## 🤖 CI/CD Pipeline

```
┌──────────────────┐
│  Developer       │
│  git push        │
└────────┬─────────┘
         │
         ▼
┌─────────────────────────────────────────────┐
│         GitHub Actions                      │
├─────────────────────────────────────────────┤
│                                             │
│  🔍 Detect changes:                         │
│   • launcher/* → launcher.yml              │
│   • externa/* → build.yml                   │
│   • launcher-server/* → deploy-server.yml  │
│                                             │
└─┬───────────────┬───────────────┬───────────┘
  │               │               │
  │ launcher.yml  │ build.yml     │ deploy-server.yml
  │               │               │
  ▼               ▼               ▼
┌──────────┐  ┌──────────┐  ┌─────────────┐
│ Compile  │  │ Compile  │  │ POST        │
│ launcher │  │ externa  │  │ /api/admin/ │
│  .exe    │  │  .exe    │  │ reload      │
└────┬─────┘  └────┬─────┘  └──────┬──────┘
     │             │                │
     │ POST        │ POST           │ Server
     │ /ci/upload  │ /ci/upload     │ git pull
     │             │                │ restart
     ▼             ▼                ▼
┌────────────────────────────────────────┐
│         SERVER STORAGE                 │
│                                        │
│  storage/games/launcher/2.0.X.enc      │
│  storage/games/cs2/1.0.X.enc           │
│  data/launcher.db (версии)             │
└────────────────────────────────────────┘
```

---

## 🗄️ База Данных (SQLite)

### Схема

```sql
-- Пользователи
users (
  id INTEGER PRIMARY KEY,
  username TEXT UNIQUE,
  password TEXT,              -- bcrypt hash
  hwid TEXT UNIQUE,           -- привязка к железу
  is_admin INTEGER DEFAULT 0,
  created_at TEXT,
  last_login TEXT
)

-- Лицензии
licenses (
  id INTEGER PRIMARY KEY,
  user_id INTEGER,            -- NULL = не активирован
  game_id TEXT,               -- "cs2", "dayz", etc.
  license_key TEXT UNIQUE,
  expires_at TEXT,            -- NULL = lifetime
  created_at TEXT
)

-- Игры
games (
  id TEXT PRIMARY KEY,        -- "cs2", "launcher"
  name TEXT,
  description TEXT,
  latest_version TEXT,
  is_active INTEGER DEFAULT 1,
  created_at TEXT
)

-- Статусы игр
game_status (
  game_id TEXT PRIMARY KEY,
  status TEXT,                -- operational/updating/maintenance
  message TEXT,
  updated_at TEXT,
  updated_by TEXT
)

-- Версии читов
versions (
  id INTEGER PRIMARY KEY,
  game_id TEXT,
  version TEXT,
  changelog TEXT,
  file_path TEXT,
  file_hash TEXT,
  created_at TEXT
)
```

---

## 🔐 Шифрование Файлов

### Алгоритм
- **AES-256-CBC**
- **IV:** 16 байт (рандомный)
- **Key:** из `ENCRYPTION_KEY` в .env

### Структура файла
```
[16 bytes IV][Encrypted data...]
```

### Шифрование (сервер)
```javascript
const iv = crypto.randomBytes(16);
const cipher = crypto.createCipheriv('aes-256-cbc', key, iv);
const encrypted = Buffer.concat([iv, cipher.update(data), cipher.final()]);
```

### Расшифровка (сервер при скачивании)
```javascript
const iv = encrypted.slice(0, 16);
const data = encrypted.slice(16);
const decipher = crypto.createDecipheriv('aes-256-cbc', key, iv);
const decrypted = Buffer.concat([decipher.update(data), decipher.final()]);
```

---

## 📡 API Endpoints - Полный Список

### Public (без токена)
```
GET  /                              - главная страница
GET  /api/games/status              - статус всех игр
GET  /api/download/launcher         - скачать лаунчер
POST /api/auth/login                - логин
POST /api/auth/register             - регистрация
```

### Authenticated (JWT token)
```
GET  /api/auth/me                   - профиль пользователя
POST /api/auth/activate             - активировать ключ
GET  /api/games                     - список игр с лицензиями
GET  /api/download/:game/external   - скачать чит
```

### Admin (JWT + is_admin)
```
GET  /api/admin/stats               - статистика
GET  /api/admin/users               - список пользователей
POST /api/admin/licenses            - создать ключи
DELETE /api/admin/licenses/:key     - удалить ключ
POST /api/admin/games/:id/status    - изменить статус
POST /api/admin/users/:id/extend    - продлить лицензию
DELETE /api/admin/users/:id/hwid    - сбросить HWID
```

### CI/CD (X-CI-API-Key header)
```
POST /api/admin/ci/upload           - загрузить билд
POST /api/admin/ci/offsets/:game    - обновить оффсеты
POST /api/admin/reload              - перезагрузить сервер
```

---

## 🎨 UI/UX Лаунчера

### Экраны
1. **Splash** - загрузка (2 секунды)
2. **Login** - вход или регистрация
3. **Register** - создание аккаунта
4. **Main** - список игр + управление

### Компоненты
- `DrawWindowControls()` - кнопки _ X (minimize/close)
- `StyledInput()` - поля ввода
- `StyledButton()` - кнопки с градиентами
- `RenderLogin()` - экран логина
- `RenderMain()` - главное окно

### Темы
```cpp
namespace theme {
    accent = (140, 90, 245)      // фиолетовый
    success = (80, 220, 140)     // зелёный
    error = (255, 100, 100)      // красный
    text = (240, 240, 250)       // белый
    textDim = (150, 150, 170)    // серый
}
```

---

## 🔄 Синхронизация Версий

### Как работает версионирование

```
┌──────────────────┐
│ GitHub Actions   │
│ env.VERSION:     │
│ 2.0.${{run_num}} │ ◄─── Увеличивается автоматически
└────────┬─────────┘
         │
         │ -DLAUNCHER_VERSION=2.0.47
         ▼
┌──────────────────┐
│ CMake            │
│ Передаёт в код   │
└────────┬─────────┘
         │
         │ target_compile_definitions(...)
         ▼
┌──────────────────┐
│ main.cpp         │
│ LAUNCHER_VERSION │ ◄─── Теперь "2.0.47"
│ показывает в UI  │
└──────────────────┘
         │
         │ POST /ci/upload (version=2.0.47)
         ▼
┌──────────────────┐
│ База данных      │
│ games.           │
│ latest_version   │ ◄─── "2.0.47"
└──────────────────┘
         │
         │ GET /api/games/status
         ▼
┌──────────────────┐
│ Launcher читает  │
│ сравнивает:      │
│ 2.0.46 < 2.0.47? │ ◄─── Если да → Update Required
└──────────────────┘
```

---

## 📦 Структура Storage на Сервере

```
storage/
├── games/
│   ├── launcher/
│   │   └── 2.0.47_<timestamp>_launcher.exe.enc
│   └── cs2/
│       └── 1.0.X_<timestamp>_externa.exe.enc
└── offsets/
    ├── cs2.json
    ├── cs2_info.json
    └── dayz.json
```

---

## 🛡️ Безопасность

### Уровни защиты

#### Launcher
1. **VM Detection** - определяет виртуалки
2. **Debugger Check** - блокирует отладку
3. **HWID Lock** - один аккаунт = одно железо
4. **Integrity Check** - проверка целостности (опционально)

#### Server
1. **JWT Auth** - токены с истечением
2. **Rate Limiting** - защита от DDoS
3. **File Encryption** - AES-256-CBC
4. **HWID Verification** - двойная проверка
5. **SQL Injection** - prepared statements

#### Network
1. **HTTPS** - SSL через Certbot/Cloudflare
2. **CORS** - ограничение origin
3. **Helmet.js** - security headers
4. **Nginx** - reverse proxy

---

## 🔧 Переменные Окружения (.env)

```env
# Server
PORT=3000
HOST=127.0.0.1
NODE_ENV=production

# Security
JWT_SECRET=<64 символа, рандомные>
ENCRYPTION_KEY=<32 символа для AES>
CI_API_KEY=<секретный ключ для GitHub Actions>

# Storage
STORAGE_PATH=../storage

# Telegram (опционально)
TELEGRAM_BOT_TOKEN=<токен бота>
TELEGRAM_ADMIN_ID=<ваш Telegram ID>
```

---

## 📊 Мониторинг

### Health Check
```bash
curl http://single-project.duckdns.org/health
# → {"status":"ok","version":"1.0.0"}
```

### Логи
```bash
# Сервис
journalctl -u launcher -f

# Только ошибки
journalctl -u launcher -p err --since "1 hour ago"

# Последние 100 строк
journalctl -u launcher -n 100
```

### Метрики
```javascript
GET /api/admin/stats
{
  "users": 145,
  "games": 1,
  "licenses": 89,
  "downloads_today": 12
}
```

---

**Последнее обновление:** 23.12.2025

