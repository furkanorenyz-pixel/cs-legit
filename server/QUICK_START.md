# 🚀 Быстрый старт - БЕСПЛАТНЫЙ сервер

## ✅ Вариант 1: Railway.app (САМЫЙ ПРОСТОЙ)

**Бесплатно:** 500 часов/месяц (хватит на всегда работающий сервер)

### Шаги:

1. **Зарегистрируйся:** https://railway.app
2. **Создай проект:**
   - New Project → Deploy from GitHub
   - Выбери свой репозиторий
3. **Настрой:**
   - Root Directory: `server`
   - Start Command: `npm start`
   - Port: Railway сам назначит (через переменную `PORT`)
4. **Готово!** Сервер работает на `https://your-app.railway.app`

**Что хранится:**
- `externa.exe` (CS2 external)
- `hv_internal.dll` (CS2 internal)
- `hv_injector.exe` (инжектор)
- Манифесты с версиями

**Стоимость:** $0/месяц (если не превышаешь лимиты)

---

## ✅ Вариант 2: Render.com

**Бесплатно:** 750 часов/месяц

### Шаги:

1. **Зарегистрируйся:** https://render.com
2. **New → Web Service**
3. **Настрой:**
   - Build Command: `cd server && npm install`
   - Start Command: `cd server && npm start`
4. **Готово!** Сервер на `https://your-app.onrender.com`

**Стоимость:** $0/месяц

---

## ✅ Вариант 3: Fly.io

**Бесплатно:** 3 shared-cpu VMs

### Шаги:

```bash
# Установи flyctl
curl -L https://fly.io/install.sh | sh

# Логин
fly auth login

# Деплой
cd server
fly launch
```

**Стоимость:** $0/месяц

---

## 💰 Вариант 4: VPS (если нужен полный контроль)

**Цена:** $5-10/месяц (DigitalOcean, Hetzner, Vultr)

### Что получаешь:
- Полный контроль
- Больше места (20-50GB)
- Можно настроить Nginx + SSL
- Своё доменное имя

---

## 📦 Что именно хранится на сервере?

**НЕ образы дисков, а файлы читов:**

```
files/
├── cs2/
│   ├── external/
│   │   └── v1.0.0/
│   │       ├── externa.exe          ← Исполняемый файл
│   │       └── manifest.json        ← Метаданные
│   └── internal/
│       └── v1.0.0/
│           ├── hv_internal.dll      ← DLL для инжекта
│           ├── hv_injector.exe      ← Инжектор
│           └── manifest.json
└── dayz/                             ← Для других игр
    └── external/
        └── v1.0.0/
            └── dayz_cheat.exe
```

**Размер:** Обычно 5-50MB на версию

---

## 🎯 Рекомендация

**Начни с Railway.app:**
- ✅ Бесплатно
- ✅ Автоматический деплой из GitHub
- ✅ HTTPS из коробки
- ✅ Не нужно настраивать сервер

**Когда понадобится VPS:**
- Если нужно > 100GB места
- Если нужен полный контроль
- Если нужны кастомные настройки

---

## 📝 Пример использования

1. **Загрузи файлы на сервер:**
```bash
curl -X POST https://your-server.railway.app/api/upload \
  -F "game=cs2" \
  -F "type=external" \
  -F "version=v1.0.0" \
  -F "files=@externa.exe"
```

2. **Лаунчер проверяет обновления:**
```
GET https://your-server.railway.app/api/check-update?game=cs2&type=external&currentVersion=v0.9.0
```

3. **Лаунчер скачивает новую версию:**
```
GET https://your-server.railway.app/files/cs2/external/v1.0.0/externa.exe
```

---

## ❓ FAQ

**Q: Нужно ли платить?**  
A: Нет, если используешь бесплатные варианты (Railway/Render)

**Q: Сколько места нужно?**  
A: ~100-500MB для всех версий всех игр

**Q: Можно ли использовать свой домен?**  
A: Да, в Railway/Render можно подключить свой домен

**Q: Безопасно ли хранить читы на публичном сервере?**  
A: Да, это просто файлы. Можно добавить аутентификацию (см. DEPLOY.md)

**Q: Что если сервер упадет?**  
A: Railway/Render автоматически перезапускают. Можно настроить мониторинг.

