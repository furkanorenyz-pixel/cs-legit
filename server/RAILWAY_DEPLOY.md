# 🚂 Деплой на Railway.app (5 минут)

## Шаг 1: Регистрация

1. Перейди на https://railway.app
2. Нажми **"Start a New Project"**
3. Войди через GitHub (проще всего)

## Шаг 2: Подключение репозитория

1. Выбери **"Deploy from GitHub repo"**
2. Выбери свой репозиторий `externa-cheat`
3. Railway автоматически определит Node.js

## Шаг 3: Настройка

1. В настройках проекта:
   - **Root Directory:** `server`
   - **Start Command:** `npm start` (Railway сам определит)
   - **Port:** Railway сам назначит (через переменную `PORT`)

2. Railway автоматически:
   - Установит зависимости (`npm install`)
   - Запустит сервер
   - Назначит URL типа `https://your-app.railway.app`

## Шаг 4: Проверка

Открой в браузере:
```
https://your-app.railway.app/api/games
```

Должен вернуться:
```json
{
  "games": []
}
```

## Шаг 5: Загрузка первого файла

```bash
# Замени YOUR-APP на свой URL
curl -X POST https://YOUR-APP.railway.app/api/upload \
  -F "game=cs2" \
  -F "type=external" \
  -F "version=v1.0.0" \
  -F "description=CS2 External ESP" \
  -F "files=@externa.exe"
```

## Готово! ✅

Теперь лаунчер может:
- Проверять обновления: `GET /api/check-update?game=cs2&type=external`
- Скачивать файлы: `GET /files/cs2/external/v1.0.0/externa.exe`

---

## 💡 Полезные ссылки

- **Dashboard:** https://railway.app/dashboard
- **Логи:** В Railway dashboard → View Logs
- **Переменные:** Settings → Variables (для секретов)

## 🔒 Добавить свой домен (опционально)

1. Settings → Domains
2. Add Custom Domain
3. Настрой DNS записи (Railway покажет инструкцию)

## 📊 Мониторинг

Railway показывает:
- Использование CPU/RAM
- Трафик
- Логи в реальном времени

## 💰 Стоимость

**Бесплатно:**
- 500 часов/месяц
- $5 кредитов/месяц
- Хватит для небольшого проекта

**Если превысишь:**
- $0.000463/час за CPU
- $0.000231/GB за RAM
- Очень дешево для простого API

